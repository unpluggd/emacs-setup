;; (packages-install
;;     (cons 'csv-mode marmalade))
;; (packages-install
;;     (cons 'csv-nav melpa))

;; (setq auto-mode-alist (cons '("\\.csv" . csv-mode) auto-mode-alist))
;; (setq auto-mode-alist (cons '("\\.csv" . csv-nav) auto-mode-alist))
